Format:

MODE #channel +i
MODE #channel +t
MODE #channel +k key
MODE #channel +o nick
MODE #channel +l limit
MODE #channel -i


Mandatory modes:

Mode	Meaning
+i	Invite only
+t	Only ops can set topic
+k	Channel password
+o	Give operator
+l	User limit

Only operators can change modes.